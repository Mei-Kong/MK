.decode('')    可指定输出的字符编码

isinstance( object 对象, classinfo 类型 )    判断一个对象的类型，如果object对象 是 classinfo类型的实例或是一个类型子类的实例，则返回true

.add() 方法用于给集合添加元素，如果添加的元素在集合中已存在，则不执行任何操作。

.save()  保存 (’文件名‘, ‘内容’)
.load()  读取 ('文件名')

sep  对于每个输出的字符流之间用sep的值分隔    #print('A','B','C',sep='_')   输出:A_B_C